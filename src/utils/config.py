# fmt: off
SD_URL = "http://localhost:7860"

APP_DEFAULTS = dict(
    # font = "arial.ttf",
    font = "Pillow/Tests/fonts/FreeMono.ttf",
    font_size = 40,
    font_color = (0,0,0),

    # Space around each image
    padding = 30,

    # Space around header cells
    header_padding = 60,

    # Size of separator for row / column header cells
    border_size = 5,

    # Color of separator for header cells
    border_color = (0, 0, 0),

    # Background color
    background_color = (200, 200, 200),
)
